name: evoyou_ia
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.1

  image_picker: ^1.1.2
  image_cropper: ^9.0.0
  path_provider: ^2.1.5

  google_maps_flutter: ^2.12.3
  geolocator: ^14.0.0
  permission_handler: ^12.0.1

  google_mlkit_face_detection: ^0.13.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/imagenes/ui/
    - assets/imagenes/exercises/
    - assets/imagenes/muscles/
    - assets/imagenes/food/